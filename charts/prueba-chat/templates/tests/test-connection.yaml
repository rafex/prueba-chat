apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "juego-aviones.fullname" . }}-test-connection"
  labels:
    {{- include "juego-aviones.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "juego-aviones.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
