apiVersion: v1
kind: Service
metadata:
  name: {{ include "juego-aviones.fullname" . }}
  labels:
    {{- include "juego-aviones.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "juego-aviones.selectorLabels" . | nindent 4 }}
